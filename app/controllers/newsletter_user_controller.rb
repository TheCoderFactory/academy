class NewsletterUserController < ApplicationController
  # def create
    # @newsletter_user = NewsletterUser.create(newsletter_user_params)
    # if (@newsletter_user.save)
      # @newsletter_user.subscribe_to_mailchimp
      # redirect_to controller: "pages", action: "confirmation", type: "news_conf"
    # else
      # redirect_to controller: "pages", action: "confirmation", type: "news_fail"
    # end
  # end

  # private
  # def newsletter_user_params
    # params.require(:newsletter_user).permit(:first_name, :last_name, :email)
  # end
end
